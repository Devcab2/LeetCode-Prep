// code the ${color} in the return line and call the function so that the message reads Jerry always wears blue shirts.   pass the parameter without parenthesis. 

const bestColor = color => {
  const name = `Jerry`;
  return `${name} always wears ${color} shirts.`;
};

console.log(bestColor`blue`);

